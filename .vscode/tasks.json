{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "command": "yarn build",
            "problemMatcher": "$tsc",
            "group": "build",
            "label": "tsc: build - tsconfig.json"
        }
    ]
}