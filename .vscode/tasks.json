{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "command": "tsc -p src",
            "problemMatcher": [
                "$tsc"
            ],
            "group": "build",
            "label": "tsc: 构建 - tsconfig.json"
        }
    ]
}